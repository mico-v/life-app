{
  "name": "life-app-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply LIFE_APP_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply LIFE_APP_DB --remote"
  },
  "dependencies": {
    "hono": "^4.6.10"
  },
  "devDependencies": {
    "wrangler": "^3.86.1"
  }
}
